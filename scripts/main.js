const ApiKey = require("./api_key");

class MovieFinder {
    constructor() {}
}


document.addEventListener('DOMContentLoaded', () => {
    console.log(ApiKey);
    const meteo = new MovieFinder();
});